DataCollectorAgent fetches company news and stock data (dummy or API).

AnalystAgent takes that data and generates insights & risk factors.

Orchestrator runs the agents sequentially.

Streamlit UI allows you to input company ticker and shows results with history.